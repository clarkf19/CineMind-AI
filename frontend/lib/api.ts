import { Movie } from "../types/movie";

export const fetchMovieSearch = async (query: string): Promise<Movie[]> => {
  // Placeholder API stub for future backend integration.
  // Replace with a real fetch to /api/search once the backend is ready.
  return [];
};
